[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd backend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'Backend only - skipping frontend build'"]

[start]
cmd = "cd backend && node server.js"
